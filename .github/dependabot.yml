version: 2
updates:
  # Security updates are enabled at the repository level; this keeps ordinary
  # version drift small so a security bump is never also a major upgrade.
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
